# L2 — Detailed Requirements

---

## L1-1: Data Mining Service

### L2-1.1: Event Source Abstraction

The service shall define an `IEventSource` interface so new data sources can be added without modifying existing code.

**Acceptance Criteria:**
- An `IEventSource` interface exists with a method that returns discovered events.
- At least three concrete implementations exist (Eventbrite, Meetup, WebSearch).
- A unit test verifies each source can be instantiated and implements the interface.

### L2-1.2: Eventbrite Source

An `EventbriteEventSource` shall query the Eventbrite API for events matching keywords related to Black professionals in space/defense (e.g., "Black in aerospace", "BEYA STEM", "National Society of Black Engineers defense", "Blacks in Government space").

**Acceptance Criteria:**
- The source sends HTTP requests to the Eventbrite API with relevant search terms.
- Queries are scoped to the Toronto/Mississauga area using location parameters (latitude/longitude or city name).
- Returned events are mapped to the shared `DiscoveredEvent` model.
- A test verifies the mapping logic with a stubbed HTTP response.

### L2-1.3: Meetup Source

A `MeetupEventSource` shall query the Meetup.com GraphQL API for groups and events matching relevant keywords.

**Acceptance Criteria:**
- The source sends GraphQL queries to the Meetup API.
- Queries are scoped to the Toronto/Mississauga area using location parameters.
- Returned events are mapped to the shared `DiscoveredEvent` model.
- A test verifies the mapping logic with a stubbed HTTP response.

### L2-1.4: Web Search Source

A `WebSearchEventSource` shall use a web search API (Bing Search API) to find event pages matching relevant queries.

**Acceptance Criteria:**
- The source sends HTTP requests to the Bing Search API with relevant search terms.
- Search queries include "Toronto" or "Mississauga" as geographic qualifiers.
- Returned results are mapped to the shared `DiscoveredEvent` model.
- A test verifies the mapping logic with a stubbed HTTP response.

### L2-1.5: Geographic Configuration

All event sources shall use a shared geographic configuration that defines the target area as Toronto and Mississauga, Ontario, Canada. The configuration shall include city names, latitude/longitude coordinates (Toronto: 43.6532, -79.3832; Mississauga: 43.5890, -79.6441), and a search radius (default: 50 km).

**Acceptance Criteria:**
- A `MiningOptions` configuration class exists with geographic properties (Cities, Latitude, Longitude, RadiusKm).
- Default values target the Toronto/Mississauga area.
- Configuration is bindable from `appsettings.json` under a `Mining` section.
- A test verifies default configuration values.

### L2-1.6: Mining Orchestrator Background Service

A `MiningBackgroundService` (hosted service) shall periodically invoke all registered `IEventSource` implementations with the configured geographic parameters, deduplicate results, and persist them via the data access layer.

**Acceptance Criteria:**
- The service runs on a configurable interval (default: every 6 hours).
- Duplicate events (same URL or name+date) are not re-inserted.
- A test verifies deduplication logic.

---

## L1-2: Data Store

### L2-2.1: Entity Model

The data layer shall define an `Event` entity with properties: Id, Title, Description, Url, Location, StartDate, EndDate, Source, Tags, RelevanceScore, CreatedAt, UpdatedAt.

**Acceptance Criteria:**
- The `Event` entity class exists with all listed properties.
- A unit test verifies all properties are present.

### L2-2.2: EF Core DbContext

A `ConstellationDbContext` shall be configured with an `Events` DbSet and use SQL Server as the provider.

**Acceptance Criteria:**
- The DbContext class exists and configures the Events table.
- An initial EF Core migration can be generated successfully.
- A test verifies the DbContext can be instantiated with an in-memory provider.

### L2-2.3: Event Repository

An `IEventRepository` / `EventRepository` pair shall encapsulate data access operations: Add, GetById, GetAll (with paging), Search (by keyword, date range, source, tags).

**Acceptance Criteria:**
- CRUD and search methods are implemented.
- A test verifies add and search operations using the in-memory database provider.

---

## L1-3: Web API

### L2-3.1: Events Controller

An `EventsController` shall expose the following endpoints:
- `GET /api/events` — list events with paging, filtering by keyword, date range, source, and tags.
- `GET /api/events/{id}` — get a single event by ID.

**Acceptance Criteria:**
- Both endpoints return correct HTTP status codes (200, 404).
- Paging defaults to page 1, size 20.
- A test verifies each endpoint using `WebApplicationFactory`.

### L2-3.2: Search Endpoint

An `EventsController` shall expose:
- `GET /api/events/search?q={query}` — full-text keyword search across Title, Description, Tags.

**Acceptance Criteria:**
- Returns matching events with 200 OK.
- Returns empty array with 200 OK when no matches.
- A test verifies search behavior using `WebApplicationFactory`.

### L2-3.3: Sources Controller

A `SourcesController` shall expose:
- `GET /api/sources` — list all distinct event sources with event counts.

**Acceptance Criteria:**
- Returns a list of source names with their event counts.
- A test verifies the endpoint using `WebApplicationFactory`.

### L2-3.4: Health Check Endpoint

The API shall expose a `GET /health` endpoint.

**Acceptance Criteria:**
- Returns 200 OK with a JSON body containing status and timestamp.
- A test verifies the health endpoint.

---

## L1-4: Solution Structure

### L2-4.1: Solution Layout

The solution shall be organized as:
```
Constellation.sln
src/
  Constellation.Api/          — ASP.NET Core Web API (Controllers, Program.cs)
  Constellation.Services/     — Data mining services and background workers
  Constellation.Data/         — EF Core DbContext, migrations, repositories
  Constellation.Models/       — Shared DTOs and entity models
tests/
  Constellation.Tests/        — Unit and integration tests
```

**Acceptance Criteria:**
- The solution file references all five projects.
- Each project builds successfully.
- `dotnet build` succeeds at the solution level.

### L2-4.2: Dependency Injection Configuration

The API project's `Program.cs` shall register all services (repositories, event sources, background service, DbContext) via dependency injection.

**Acceptance Criteria:**
- All services are registered in the DI container.
- The application starts without DI resolution errors.
- A test verifies the application can be built via `WebApplicationFactory`.

---

## L1-5: UI Design

### L2-5.1: App Shell and Navigation

The UI shall have an Angular Material dark-mode app shell consisting of a `mat-toolbar` top app bar with the Constellation brand name and a `mat-sidenav` side navigation with links to Dashboard, Events, Sources, and Settings. Typography shall use Material Design type scale.

**Acceptance Criteria:**
- A dark-themed top toolbar displays the "Constellation" brand name with a menu toggle icon button.
- A side navigation drawer contains nav items for Dashboard, Events, Sources, and Settings, each with a Material icon.
- The layout uses proper Material dark-mode surface colors (#121212 background, #1E1E1E surfaces).
- All text uses the Material Design type scale (Roboto font family).

### L2-5.2: Dashboard Screen

The UI shall have a Dashboard screen showing summary stat cards (Total Events, Sources Active, Events This Week, Avg Relevance Score) using `mat-card` components, followed by a "Recent Events" section with a `mat-table` showing the latest events.

**Acceptance Criteria:**
- Four `mat-card` stat cards are displayed in a responsive grid row, each with a Material icon, metric value, and label.
- A "Recent Events" `mat-table` displays columns: Title, Date, Source, Relevance with sample data rows.
- Cards use Material elevation and dark surface colors.
- A `mat-raised-button` "View All Events" appears below the table.

### L2-5.3: Events List Screen

The UI shall have an Events screen with a search/filter bar and a paginated event list. The filter bar uses `mat-form-field` with `mat-input` for keyword search, `mat-select` for source filter, and `mat-datepicker` for date range. Events are displayed as `mat-card` items showing title, date, source chip, relevance badge, and description excerpt.

**Acceptance Criteria:**
- A filter bar contains a search input, source dropdown, and date range picker, all using Angular Material form field components.
- Event cards display title, date, source as a `mat-chip`, relevance as a colored badge, and a truncated description.
- A `mat-paginator` appears at the bottom of the event list.
- All form controls use Material dark-mode theming (filled appearance).

### L2-5.4: Event Detail View

The UI shall have an Event Detail view displayed as a full-width `mat-card` with the event title, full description, metadata (date, location, source, relevance score), tags as `mat-chip-set`, and action buttons (Open Link, Share) using `mat-raised-button` and `mat-stroked-button`.

**Acceptance Criteria:**
- The detail card displays the event title as a `mat-card-title` with headline typography.
- Metadata fields (Date, Location, Source, Relevance) are listed with Material icons.
- Tags are displayed as a `mat-chip-set` with individual `mat-chip` components.
- Two action buttons appear: "Open Link" (raised, accent color) and "Share" (stroked).

### L2-5.5: Sources Overview Screen

The UI shall have a Sources screen showing each data source as a `mat-card` with the source name, event count, last sync timestamp, and a status indicator. A `mat-progress-bar` shows the relative proportion of events from each source.

**Acceptance Criteria:**
- Each source is displayed as a `mat-card` with source name, event count, last synced time, and a status chip (Active/Inactive).
- A `mat-progress-bar` inside each card visualizes the event count relative to total.
- Cards are arranged in a grid layout with proper spacing.
- Uses Material dark-mode card styling.

---

## L1-6: Frontend Domain Models

### L2-6.1: Event Model Interface

The domain library shall export a TypeScript `EventDto` interface matching the backend DTO with properties: id, title, description, url, location, startDate, endDate, source, tags (string array), relevanceScore.

**Acceptance Criteria:**
- An `EventDto` interface exists in `projects/domain/src/lib/models/event.model.ts`.
- All properties match the backend `EventDto` C# class (using camelCase).
- The interface is exported from the library's public API.
- A unit test verifies the interface can be used to type an object.

### L2-6.2: Search and Paging Model Interfaces

The domain library shall export `EventSearchRequest`, `PagedResult<T>`, and `SourceSummaryDto` interfaces matching their backend counterparts.

**Acceptance Criteria:**
- `EventSearchRequest` interface has optional keyword, fromDate, toDate, source, tag, and required page/pageSize.
- `PagedResult<T>` interface has items (T[]), totalCount, page, pageSize, totalPages.
- `SourceSummaryDto` interface has source and eventCount.
- All interfaces are exported from the library's public API.
- A unit test verifies each interface can be used for typing.

---

## L1-7: Frontend API Services

### L2-7.1: Events API Service

The api library shall provide an `EventsService` injectable service with methods: `getEvents(params)`, `getEvent(id)`, and `searchEvents(query)` that call the corresponding backend endpoints and return typed observables.

**Acceptance Criteria:**
- `EventsService` is an `@Injectable({ providedIn: 'root' })` service.
- `getEvents` calls `GET /api/events` with query parameters and returns `Observable<PagedResult<EventDto>>`.
- `getEvent` calls `GET /api/events/{id}` and returns `Observable<EventDto>`.
- `searchEvents` calls `GET /api/events/search?q={query}` and returns `Observable<EventDto[]>`.
- A unit test verifies each method constructs the correct HTTP request using `HttpClientTestingModule`.

### L2-7.2: Sources API Service

The api library shall provide a `SourcesService` injectable service with a `getSources()` method that calls `GET /api/sources` and returns typed observables.

**Acceptance Criteria:**
- `SourcesService` is an `@Injectable({ providedIn: 'root' })` service.
- `getSources` calls `GET /api/sources` and returns `Observable<SourceSummaryDto[]>`.
- A unit test verifies the method constructs the correct HTTP request.

---

## L1-8: Frontend Presentational Components

### L2-8.1: Sidenav Component

A `SidenavComponent` shall render a vertical navigation sidebar with the Constellation brand logo/text and nav items (Dashboard, Events, Sources, Settings), each with a Material icon. The active item shall be visually highlighted.

**Acceptance Criteria:**
- Component accepts `activeRoute` input to highlight the current nav item.
- Component emits `navItemClicked` output with the route string when a nav item is clicked.
- Renders brand icon (stars) and "Constellation" text at top.
- Four nav items with icons: dashboard, event, cloud_sync, settings.
- Active item has highlighted background (#2C2C2C) and accent-colored icon (#BB86FC).
- Uses dark surface color (#1A1A1A) background.
- A unit test verifies the component renders and emits navigation events.

### L2-8.2: Stat Card Component

A `StatCardComponent` shall render a summary statistic card with an icon, metric value, label, and trend indicator.

**Acceptance Criteria:**
- Component accepts inputs: `icon` (string), `value` (string), `label` (string), `trend` (string), `trendUp` (boolean).
- Renders a Material icon in a circular container.
- Displays the metric value in large text and label below.
- Shows trend with a trending_up/trending_down icon and colored text (green for up, red for down).
- Uses dark card styling (#1E1E1E background, 12px corner radius, 24px padding).
- A unit test verifies the component renders all inputs correctly.

### L2-8.3: Toolbar Component

A `ToolbarComponent` shall render the top app bar with a menu toggle button, the "Constellation" brand title, and action icons (search, notifications, avatar).

**Acceptance Criteria:**
- Component emits `menuToggled` output when menu icon is clicked.
- Renders menu icon, "Constellation" title, search icon, notification icon, and avatar circle.
- Uses dark toolbar color (#1F1F1F) with bottom border.
- A unit test verifies the component renders and emits the toggle event.

### L2-8.4: Event Card Component

An `EventCardComponent` shall render an event list item card showing the event title, relevance badge, metadata row (date, location, source chip), and description excerpt.

**Acceptance Criteria:**
- Component accepts an `event` input of type `EventDto`.
- Component emits `eventClicked` output with the event when clicked.
- Displays title and relevance score badge in the header row.
- Shows date with calendar icon, location with pin icon, and source as a chip.
- Truncated description text below metadata.
- Uses dark card styling (#1E1E1E, 12px radius, 24px padding).
- A unit test verifies the component renders event data and emits click events.

### L2-8.5: Source Card Component

A `SourceCardComponent` shall render a data source card with source name, status chip, event count, last sync time, and a progress bar showing event share.

**Acceptance Criteria:**
- Component accepts inputs: `name`, `icon`, `iconColor`, `eventCount`, `lastSync`, `status`, `sharePercent`.
- Displays source icon in a circle, name, and Active/Inactive status chip.
- Shows event count and last sync as stat columns.
- Progress bar with colored fill representing the share percentage.
- Uses dark card styling (#1E1E1E, 12px radius, 24px padding).
- A unit test verifies the component renders all data correctly.

### L2-8.6: Event Detail Card Component

An `EventDetailCardComponent` shall render a full event detail view with title, relevance badge, metadata grid, description, tags, and action buttons.

**Acceptance Criteria:**
- Component accepts an `event` input of type `EventDto`.
- Component emits `openLink` and `share` outputs.
- Displays event title with star-rated relevance badge.
- Metadata grid shows date, location, source, and relevance score with Material icons.
- Full description text section.
- Tags rendered as chip pills.
- "Share" (stroked) and "Open Link" (filled teal) action buttons.
- A unit test verifies the component renders all sections and emits button events.

### L2-8.7: Data Table Component

A `DataTableComponent` shall render a tabular view of events with configurable columns and row data.

**Acceptance Criteria:**
- Component accepts `columns` input (array of column definitions) and `rows` input (EventDto array).
- Component emits `rowClicked` output with the clicked event.
- Renders header row with column labels in uppercase.
- Data rows show title, date, source chip, and relevance badge.
- Alternating row dividers for visual separation.
- Uses dark surface (#1E1E1E) with header background (#2C2C2C).
- A unit test verifies the component renders columns and rows.

### L2-8.8: Paginator Component

A `PaginatorComponent` shall render pagination controls with items-per-page selector, page info text, and previous/next buttons.

**Acceptance Criteria:**
- Component accepts inputs: `totalItems`, `pageSize`, `currentPage`.
- Component emits `pageChanged` output with the new page number and `pageSizeChanged` with the new size.
- Displays "Items per page:" with a selector, current range text, and prev/next arrow buttons.
- Disables prev button on first page and next button on last page.
- A unit test verifies pagination logic and output emissions.

---

## L1-9: Frontend Intelligent Components

### L2-9.1: App Shell Component

An `AppShellComponent` in the domain library shall compose the `ToolbarComponent` and `SidenavComponent` from the components library with a `<router-outlet>`. It shall manage sidenav visibility state and derive the active route from the Angular Router.

**Acceptance Criteria:**
- Renders `cst-toolbar` at the top and `cst-sidenav` on the left with a `<router-outlet>` for the main content area.
- Passes the current route segment to the sidenav's `activeRoute` input.
- Handles `menuToggled` from the toolbar to toggle sidenav visibility.
- Handles `navItemClicked` from the sidenav to navigate via `Router.navigate`.
- Uses the #121212 background from the design for the content area.
- A unit test verifies navigation on nav item click and toggle behavior.

### L2-9.2: Dashboard Component

A `DashboardComponent` shall fetch summary statistics and recent events on init, and render them using `StatCardComponent` and `DataTableComponent` from the components library.

**Acceptance Criteria:**
- On init, calls `EventsService.getEvents()` (page 1, size 5) and `SourcesService.getSources()`.
- Computes stat card data: Total Events (from pagedResult.totalCount), Sources Active (sources.length), Events This Week (filtered count or totalCount), Avg Relevance (computed from returned events).
- Renders four `cst-stat-card` instances in a row with correct icons: event, cloud_sync, date_range, analytics.
- Renders a "Recent Events" section with `cst-data-table` populated from the fetched events.
- A "View All Events" button navigates to `/events`.
- A "Refresh" button re-fetches data.
- Shows a loading state while data is being fetched.
- A unit test verifies data fetching and stat computation.

### L2-9.3: Events List Component

An `EventsListComponent` shall manage filter state, fetch paginated events, and render them using `EventCardComponent` and `PaginatorComponent`.

**Acceptance Criteria:**
- Renders a filter bar with a search text input, a source dropdown, and a date input.
- On filter change or pagination, calls `EventsService.getEvents()` with the current filter/page state.
- Renders event results using `cst-event-card` for each event.
- Renders `cst-paginator` at the bottom, wired to the total count and current page.
- Clicking an event card navigates to `/events/:id`.
- Displays total event count badge next to the "Events" title.
- Shows a loading state while data is being fetched.
- A unit test verifies filter-driven data fetching and navigation on card click.

### L2-9.4: Event Detail Component

An `EventDetailComponent` shall fetch a single event by ID from the route parameter and render it using `EventDetailCardComponent`.

**Acceptance Criteria:**
- Reads the `id` route parameter and calls `EventsService.getEvent(id)`.
- Renders a "Back to Events" link that navigates to `/events`.
- Renders `cst-event-detail-card` with the fetched event data.
- Handles `openLink` output by opening the event URL in a new browser tab.
- Handles `share` output by copying the event URL to the clipboard.
- Shows a loading state while the event is being fetched.
- A unit test verifies data fetching by route param and navigation behavior.

### L2-9.5: Sources Overview Component

A `SourcesOverviewComponent` shall fetch source summaries and render them using `SourceCardComponent`.

**Acceptance Criteria:**
- On init, calls `SourcesService.getSources()`.
- Computes share percentages for each source relative to the total event count.
- Maps each source to a `cst-source-card` with the correct icon, color, and computed share percent.
- Assigns source-specific icons and colors: Eventbrite (confirmation_number, #BB86FC), Meetup (groups, #03DAC6), WebSearch (travel_explore, #FFB74D).
- Displays "{N} active sources" subtitle.
- Shows a loading state while data is being fetched.
- A unit test verifies data fetching and share percentage computation.

---

## L1-10: End-to-End Playwright Tests

### L2-10.1: Playwright Setup and Fixtures

Playwright shall be installed and configured in the `Constellation.Web` project with a `playwright.config.ts`. A shared mock data fixture shall provide consistent API responses for all tests via Playwright route interception.

**Acceptance Criteria:**
- `@playwright/test` is installed as a dev dependency.
- `playwright.config.ts` exists with baseURL pointing to the dev server.
- An `e2e/fixtures/mock-data.ts` file provides typed mock responses for `/api/events`, `/api/events/{id}`, and `/api/sources`.
- A shared `e2e/fixtures/api-mock.ts` helper intercepts all API routes and returns fixture data.
- `npx playwright test` can be invoked successfully.

### L2-10.2: Page Object Models

Page object classes shall encapsulate element selectors and user actions for each screen, keeping tests readable and maintainable.

**Acceptance Criteria:**
- `AppShellPage` class encapsulates toolbar, sidenav, and navigation actions.
- `DashboardPage` class encapsulates stat cards, recent events table, and action buttons.
- `EventsListPage` class encapsulates filter bar, event cards, and paginator.
- `EventDetailPage` class encapsulates back link, detail card, metadata, tags, and action buttons.
- `SourcesOverviewPage` class encapsulates source cards and header.
- Each page object provides methods for common interactions (click, getText, getCount, etc.).

### L2-10.3: Dashboard Workflow Tests

E2E tests shall verify the complete Dashboard screen workflow.

**Acceptance Criteria:**
- Test verifies the page title "Dashboard" is rendered.
- Test verifies four stat cards are present with correct icons (event, cloud_sync, date_range, analytics).
- Test verifies stat card values are populated from API data.
- Test verifies the "Recent Events" table renders rows with Title, Date, Source, Relevance columns.
- Test verifies the "View All Events" button navigates to `/events`.
- Test verifies the "Refresh" button text is present.
- Test verifies Roboto font-family on the page title.
- Test verifies dark background color (#121212) on the content area.

### L2-10.4: Events List Workflow Tests

E2E tests shall verify the complete Events List screen workflow.

**Acceptance Criteria:**
- Test verifies the "Events" title and count badge are rendered.
- Test verifies the filter bar has a search input, source dropdown, and date input.
- Test verifies event cards render title, relevance badge, date, location, source chip, and description.
- Test verifies the paginator displays correct range text and page controls.
- Test verifies clicking an event card navigates to `/events/:id`.
- Test verifies the search input filters events (by triggering API re-fetch).
- Test verifies Material icon `search` is present in the search field.
- Test verifies card spacing (gap: 16px between event cards from the design).

### L2-10.5: Event Detail Workflow Tests

E2E tests shall verify the complete Event Detail screen workflow.

**Acceptance Criteria:**
- Test verifies the "Back to Events" link navigates to `/events`.
- Test verifies the event title and relevance badge are rendered.
- Test verifies metadata items display with correct icons (calendar_today, location_on, cloud_sync, analytics).
- Test verifies the description section text matches API data.
- Test verifies tags are rendered as chip elements.
- Test verifies "Open Link" and "Share" buttons are present.
- Test verifies the teal (#03DAC6) background on the "Open Link" button.

### L2-10.6: Sources Overview Workflow Tests

E2E tests shall verify the complete Sources Overview screen workflow.

**Acceptance Criteria:**
- Test verifies the "Sources" title and subtitle ("{N} active sources") are rendered.
- Test verifies three source cards are present with correct names (Eventbrite, Meetup, WebSearch).
- Test verifies each source card shows the correct icon (confirmation_number, groups, travel_explore).
- Test verifies event count and "Active" status chip on each card.
- Test verifies the progress bar is present on each source card.
- Test verifies source-specific icon colors (#BB86FC, #03DAC6, #FFB74D).

### L2-10.7: Navigation Workflow Tests

E2E tests shall verify cross-screen navigation via the sidenav.

**Acceptance Criteria:**
- Test verifies sidenav renders four nav items: Dashboard, Events, Sources, Settings.
- Test verifies clicking each nav item navigates to the correct URL.
- Test verifies the active nav item is highlighted (different styling than inactive items).
- Test verifies the toolbar brand text "Constellation" is visible.
- Test verifies the menu toggle hides/shows the sidenav.
- Test verifies the sidenav brand icon "stars" is present.

---

## L1-1: Data Mining Service (Continued — Tech Events Expansion)

### L2-1.7: Expand Search Terms for Tech Events

All three event sources (Eventbrite, Meetup, WebSearch) shall include additional search terms targeting .NET, Angular, and Playwright automated testing events, alongside the existing space/defense terms.

**Acceptance Criteria:**
- `EventbriteEventSource.SearchTerms` includes terms: ".NET developer meetup", "Angular meetup", "Playwright testing", "automated testing .NET".
- `MeetupEventSource.SearchTerms` includes terms: ".NET developer", "Angular developer", "Playwright automated testing".
- `WebSearchEventSource.SearchTerms` includes terms: ".NET conference event", "Angular conference event", "Playwright testing conference".
- Existing search terms are preserved (not removed).
- A test verifies the search terms arrays contain the new entries.

### L2-1.8: Support Online/Virtual Events Without Geographic Restriction

Event sources shall run each search term twice: once with the configured geographic scope (for in-person events near L5A 4E6) and once without geographic restrictions but with an "online" or "virtual" qualifier (for remote events). The geographic center shall be updated to L5A 4E6 (Mississauga, ON: lat 43.5890, lon -79.6441).

**Acceptance Criteria:**
- `MiningOptions` defaults are updated: Latitude = 43.5890, Longitude = -79.6441 (centered on L5A 4E6 postal code area).
- `appsettings.json` Mining section is updated with the new coordinates.
- Each event source performs an additional search pass per term appending "online" or "virtual" to the query, without geographic filters (no lat/lon/radius for Eventbrite, no lat/lon for Meetup, no city qualifier for WebSearch).
- Online events have their Location set to "Online" when no venue is provided.
- A test verifies that both local and online discovery paths are invoked.

### L2-1.9: Seed Data for Tech Events (Superseded by L1-11)

> **Note:** This requirement is now fulfilled by the Playwright scraper (L1-11) which seeds the database with real scraped events rather than static seed data.

The `DatabaseSeeder` shall include representative seed events for .NET, Angular, and Playwright topics — both in-person (near Mississauga/Toronto) and online.

**Acceptance Criteria:**
- At least 3 new seed events are added covering .NET, Angular, and Playwright topics.
- At least 1 seed event has Location = "Online" representing a virtual event.
- At least 1 seed event has a Location in the Toronto/Mississauga area.
- Each new seed event has appropriate tags (e.g., ".NET", "Angular", "Playwright", "Testing").
- The application starts and seeds successfully with `dotnet run`.

---

## L1-11: Playwright-Based Event Scraper

### L2-11.1: Scraper Console Project

A new `Constellation.Scraper` console project shall be added to the solution. It shall reference the `Constellation.Data` and `Constellation.Models` projects, use the Microsoft.Playwright NuGet package for browser automation, and connect to the same SQLite database as the API.

**Acceptance Criteria:**
- A `Constellation.Scraper` project exists under `src/Constellation.Scraper/`.
- The project is referenced in `Constellation.sln`.
- The project depends on `Microsoft.Playwright` NuGet package.
- The project references `Constellation.Data` and `Constellation.Models`.
- The project builds successfully with `dotnet build`.

### L2-11.2: Eventbrite Scraper

The scraper shall use Playwright to navigate to Eventbrite's search page, enter search terms for .NET, Angular, and Playwright events near L5A 4E6 (and online), and extract event data (title, date, location, URL, description) from the search results page DOM.

**Acceptance Criteria:**
- The scraper navigates to Eventbrite search with relevant tech event queries.
- Search terms include: ".NET meetup", "Angular meetup", "Playwright testing".
- Geographic search uses "Mississauga" or "L5A 4E6" as the location filter.
- A separate search pass discovers online/virtual events.
- Extracted fields: title, URL, date, location, and description (when available).
- Events are mapped to the `Event` entity model with Source = "Eventbrite-Scraper".

### L2-11.3: Meetup Scraper

The scraper shall use Playwright to navigate to Meetup.com's event search, enter search terms for .NET, Angular, and Playwright events near Mississauga/Toronto, and extract event data from the search results.

**Acceptance Criteria:**
- The scraper navigates to Meetup.com's find page with relevant tech event queries.
- Search terms include: ".NET", "Angular", "Playwright", "software testing".
- Geographic search uses "Mississauga" or nearby location.
- A separate search pass discovers online/virtual events.
- Extracted fields: title, URL, date, location, and group name.
- Events are mapped to the `Event` entity model with Source = "Meetup-Scraper".

### L2-11.4: Google Search Scraper

The scraper shall use Playwright to perform Google searches for .NET, Angular, and Playwright events near L5A 4E6 and online, extracting event links and metadata from search result snippets.

**Acceptance Criteria:**
- The scraper performs Google searches with event-related queries.
- Search terms include: ".NET conference Toronto 2026", "Angular meetup Mississauga", "Playwright testing event online".
- Extracted fields: title, URL, and snippet/description from search results.
- Events are mapped to the `Event` entity model with Source = "WebSearch-Scraper".
- The scraper handles Google's consent/cookie pages gracefully.

### L2-11.5: Deduplication and Database Seeding

The scraper shall deduplicate discovered events against existing database entries (by URL or title+date) and persist only new events to the SQLite database.

**Acceptance Criteria:**
- Before inserting, the scraper checks if an event with the same URL already exists.
- Before inserting, the scraper checks if an event with the same title and start date already exists.
- Only genuinely new events are inserted into the database.
- The scraper logs the count of new events inserted vs. duplicates skipped.
- After running, the seeded events are visible via the API (`GET /api/events`).

### L2-11.6: CLI Invocation

The scraper shall be runnable via `dotnet run --project src/Constellation.Scraper` with console output showing progress and results.

**Acceptance Criteria:**
- Running the project launches the browser, scrapes events, and exits.
- Console output shows: which source is being scraped, how many events found, how many new events inserted.
- The scraper exits with code 0 on success.
- The scraper handles network errors and timeouts gracefully (logs warning, continues to next source).

---

## L1-12: Responsive UI Design

### L2-12.1: Mobile Dashboard (402px)

The Dashboard screen shall have a mobile variant at 402px width with a single-column stacked layout. The toolbar shall be simplified (brand name + hamburger + avatar). Stat cards shall stack vertically in a single column. The Recent Events table shall become a card-based vertical list. A bottom tab bar with 4 tabs (Dashboard, Events, Sources, Settings) replaces the sidenav.

**Acceptance Criteria:**
- A "Dashboard — Mobile" frame exists at 402px width.
- Toolbar shows hamburger icon, "Constellation" brand, and avatar — no search/notification icons in toolbar.
- Four stat cards stack vertically at full width.
- Recent Events displayed as stacked cards rather than a table.
- A bottom tab bar with 4 navigation items and Material icons is present.
- Dark-mode theme variables are used consistently.

### L2-12.2: Mobile Events List (402px)

The Events List screen shall have a mobile variant at 402px width. The filter bar shall collapse to a single search input with a filter icon button that opens a filter sheet. Event cards stack vertically at full width. Paginator adapts to compact layout.

**Acceptance Criteria:**
- A "Events List — Mobile" frame exists at 402px width.
- A search bar spans full width with a trailing filter icon button.
- Event cards stack vertically at full width.
- Paginator shows compact controls (arrows + page number only).
- Bottom tab bar is present with Events tab active.

### L2-12.3: Mobile Event Detail (402px)

The Event Detail screen shall have a mobile variant at 402px width. The detail card fills the full width. Metadata items stack vertically. Action buttons span full width at the bottom.

**Acceptance Criteria:**
- A "Event Detail — Mobile" frame exists at 402px width.
- "Back to Events" link is present at the top.
- Event title and relevance badge are displayed.
- Metadata items (date, location, source, relevance) stack vertically.
- Tags wrap in a horizontal flow.
- "Open Link" and "Share" buttons span full width, stacked vertically.
- Bottom tab bar is present.

### L2-12.4: Mobile Sources Overview (402px)

The Sources Overview screen shall have a mobile variant at 402px width. Source cards stack vertically at full width with all existing content preserved.

**Acceptance Criteria:**
- A "Sources — Mobile" frame exists at 402px width.
- Source cards stack vertically at full width.
- Each card retains source name, event count, last sync, status chip, and progress bar.
- Bottom tab bar is present with Sources tab active.

### L2-12.5: Tablet Dashboard (768px)

The Dashboard screen shall have a tablet variant at 768px width. A collapsed icon-rail sidenav (64px) replaces the full sidenav. Stat cards display in a 2×2 grid. The Recent Events table is preserved but with fewer columns.

**Acceptance Criteria:**
- A "Dashboard — Tablet" frame exists at 768px width.
- An icon-rail sidenav (64px wide) shows only icons without labels.
- Stat cards arranged in a 2×2 grid layout.
- Recent Events table shows Title, Date, and Source columns (Relevance hidden).
- Dark-mode theme variables are used consistently.

### L2-12.6: Tablet Events List (768px)

The Events List screen shall have a tablet variant at 768px width with the icon-rail sidenav and a compact filter bar.

**Acceptance Criteria:**
- A "Events List — Tablet" frame exists at 768px width.
- Icon-rail sidenav (64px) is present with Events icon highlighted.
- Filter bar shows search input and source dropdown inline; date picker hidden.
- Event cards display at full content width.
- Paginator is present.

### L2-12.7: Tablet Event Detail (768px)

The Event Detail screen shall have a tablet variant at 768px width with the icon-rail sidenav and adapted detail card layout.

**Acceptance Criteria:**
- A "Event Detail — Tablet" frame exists at 768px width.
- Icon-rail sidenav is present.
- Event detail card fills available width.
- Metadata displays in a 2×2 grid rather than a single row.
- Action buttons displayed inline (not stacked).

### L2-12.8: Tablet Sources Overview (768px)

The Sources Overview screen shall have a tablet variant at 768px width. Source cards display in a 2-column grid.

**Acceptance Criteria:**
- A "Sources — Tablet" frame exists at 768px width.
- Icon-rail sidenav is present with Sources icon highlighted.
- Source cards arranged in a 2-column grid.
- All card content (name, count, sync, status, progress bar) is preserved.

---

## L1-13: Notifications, Errors, and Dialogs

### L2-13.1: Toast Notifications

The design shall include toast notification components for success, error, warning, and info states. Each toast shall display an icon, message text, and a dismiss button. Toasts appear at the top-right of the screen.

**Acceptance Criteria:**
- Four toast variants exist: success (green/$success), error (red/$error), warning (amber/$warning), info (purple/$primary).
- Each toast has a left-edge color accent bar, an icon, a title, a message, and a close icon.
- Toast width is 360px with rounded corners (12px).
- Dark surface background ($bg-surface) is used.
- All text uses $on-surface or $on-surface-medium colors.

### L2-13.2: Error States

The design shall include error state illustrations for: network/connection error, 404 not found, and empty search results. Each error state is centered in the content area with an icon, heading, description, and a retry/action button.

**Acceptance Criteria:**
- Three error state designs exist: Network Error, 404 Not Found, Empty Results.
- Each displays a large Material icon (wifi_off, error_outline, search_off), a heading, a description, and a primary action button.
- Centered layout within a content-area-sized frame.
- Uses existing theme colors ($on-surface, $on-surface-medium, $primary).

### L2-13.3: Confirmation Dialog

The design shall include a confirmation dialog for destructive actions (e.g., "Delete Event"). The dialog has a title, description, and two action buttons (Cancel, Delete) with the destructive action using $error color.

**Acceptance Criteria:**
- A modal dialog overlay frame exists with semi-transparent backdrop (#000000 50% opacity).
- Dialog card (400px wide) centered with title, description text, and two buttons.
- "Cancel" button uses stroked/outlined style.
- "Delete" button uses $error color fill.
- Dialog uses $bg-surface background with 16px corner radius.

### L2-13.4: Filter Panel Dialog

The design shall include a filter panel dialog for mobile, containing source dropdown, date range inputs, and tag chips with Apply/Reset buttons.

**Acceptance Criteria:**
- A bottom-sheet style panel (full width, rounded top corners) exists.
- Contains a "Filters" title with close button.
- Source selector, date from/to inputs, and tag chip selectors are present.
- "Reset" (stroked) and "Apply Filters" (filled primary) buttons at the bottom.
- Uses dark-mode form field styling.

### L2-13.5: Share Dialog

The design shall include a share dialog allowing users to copy an event link or share to social platforms.

**Acceptance Criteria:**
- A centered dialog (400px wide) with "Share Event" title.
- A read-only URL text field with a "Copy" icon button.
- Social share icon buttons row (at least 3 platform icons).
- "Done" button to close.
- Uses $bg-surface background with existing theme styling.
